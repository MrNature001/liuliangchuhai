# AI开发硬性规则，所有接手的AI必须遵守
> 本文件为项目底层约束，AI执行任何代码修改前，必须阅读并严格遵守，不可忽略。

1. 用户（项目所有者/接手人）无技术能力，**不会手动修改代码、敲命令、调试报错**。所有编码、文件修改、排错、测试工作全部由AI独立完成；人只提需求。

2. ❌ 严禁只产出静态UI空壳/页面骨架。所有按钮、表单、交互组件必须一次性配套完整业务逻辑、事件处理代码，禁止写"待后续实现"占位注释。UI界面和功能逻辑同步完成，交付代码可直接编译运行，遇到报错直接返回完整修复文件，不要只给片段。

3. 技术方案选型优先评估成本更低、开发简易的替代方案。
如果无法原生独立开发功能，允许搭建中转封装层：网站前端外壳由本项目提供，底层调用第三方服务接口。对外展示功能为本站自带。

4. 每次完成子任务，更新根目录 PROJECT_WORKLOG.md，更新任务进度清单。

5. 接手项目时，第一步读取 PROJECT_WORKLOG.md，读取当前进度、已完成事项、待办任务，从断点继续工作，禁止从头重建项目。
